# Zopper Test Workspace (Private)

This directory contains private Postman collections and test assets for Zopper API integration testing.
This entire directory is ignored by Git in `.gitignore` to prevent any proprietary or sensitive test data from being committed or pushed.
